import { AmountColorDirective } from './amount-color';

describe('AmountColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountColorDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
